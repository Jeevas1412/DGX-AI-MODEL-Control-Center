# 项目操作进度反馈规范

本规范为本版本环境的强制协作与验收规则。完整知识库正本保存在项目维护者的知识库“产品版本环境管理”目录中；公开仓库只保留可执行的项目副本。

每次实际操作后必须反馈：阶段与完成度、状态、本步、可复核证据、剩余项、下一步或唯一授权需求。完成度按已定义且已验收的子项计算；构建成功、测试通过、安装通过和正式发布必须分别记录，不能相互替代。

当前版本目录名为 `上线版1.0.0空环境`，但内部 `VERSION` 仍为 `0.1.0`；所有进度反馈必须如实写作“1.0.0 目标环境 / 0.1.0 当前代码”，直至版本晋级完成。
