# Keep true for the existing self-contained UI demonstration.
VITE_USE_MOCK_DATA=true
# Used only when VITE_USE_MOCK_DATA=false. This server exposes GET endpoints only.
VITE_API_BASE_URL=http://127.0.0.1:8501

# The fixed live development UI listens only on http://127.0.0.1:5173.
