# DGX AI Control Center 项目执行流程

日期：2026-07-19  
目标：为 DGX Spark 上的 NVFP4、VLM、图像模型和代理服务提供一个类似 LM Studio 的可视化管理界面，并最终交付可在同类 Windows + SSH + DGX 环境中安装、配置和使用的软件。

## 一、最终交付目标

控制台至少包含以下能力：

1. 查看 NVFP4、VLM、图像模型、8091/8092/8093 代理状态。
2. 查看系统内存、可用内存、请求队列、首 token 延迟、生成速度、前缀缓存命中率和 MTP 接受率。
3. 查看实时日志和最近错误。
4. 调整请求级参数：输出上限、工具快速模式、思考模式、并发限制和超时。
5. 调整启动级参数：上下文、KV Cache、并发序列、批处理、MTP 和前缀缓存。
6. 安全执行预热、排空、重启、停止、常驻切换和配置回滚。
7. 一键执行单发、10/20/50 并发、长上下文和工具调用测试。
8. 安装后通过图形向导完成连接配置、能力检查和受控操作；用户不需要运行项目脚本、PowerShell 或 SSH 命令。

## 二、职责划分

### WorkBuddy 负责

- 项目目录和说明文档。
- 前端静态页面、组件和样式。
- 使用模拟数据完成界面交互。
- API 类型定义和只读接口接入。
- 表单校验、配置差异预览和测试页面。
- 不直接 SSH DGX，不执行 Docker，不重启模型。

### Codex 负责

- DGX 后端控制服务。
- vLLM 指标、系统资源、Docker 和日志采集。
- 参数白名单、权限、安全重启和回滚。
- NVFP4/VLM 联合内存保护。
- 部署、systemd、自启动和真实环境验收。

## 三、总体实施阶段

| 阶段 | 内容 | 负责人 | 验收结果 | 当前状态 |
|---|---|---|---|---|
| 0 | 冻结现状、备份配置、确定端口 | Codex | 可恢复到当前稳定服务 | ✅ 已完成：只读盘点、配置快照、预检、干跑与单文件真实恢复演练均已验收 |
| 1 | UI 原型和模拟数据 | WorkBuddy | 浏览器可运行完整静态界面 | ✅ 已完成：WB-01 至 WB-04 已完成，前端生产构建通过 |
| 2 | 只读监控 API | Codex + Kimi | 显示真实状态、指标和日志 | ✅ 已完成：Overview、Requests、Logs 已完成 DGX 真实只读模式验收 |
| 3 | 参数编辑和预设 | Kimi + Codex | 可预览变更，但尚不直接执行 | ✅ 已完成（批准范围）：安全设计、差异评估、审计原型与单字段实机复验完成；无通用写接口 |
| 4 | 安全控制操作 | Codex | 支持预热、排空、重启、回滚 | ✅ 已完成（批准范围）：受控单字段变更、验证与恢复完成；通用控制能力不在本轮范围 |
| 5 | 性能测试和历史记录 | WorkBuddy + Codex | 可一键测试并比较结果 | ✅ 已完成（批准范围）：P0–P4、历史只读展示与本机持久化均已验收；真实执行控制器另列后续 |
| 6 | 部署与最终验收 | Codex | 局域网稳定访问，不影响现有 API | ✅ 已完成（受保护 LAN 范围）：Private 网络、仅本地子网防火墙、Bearer 令牌、精确 CORS 与可逆部署均已验收；现有服务对外契约未修改 |
| 7 | 可安装产品与受控 UI 操作 | Codex + WorkBuddy | 新环境安装后可通过 UI 配置、监控和执行受控动作 | 🔄 已建立：以 HY MT2 为首个端到端试点；详见 `docs/phase-7-productization-and-ui-control-plan.md`。 |

## 四、详细工作流程

## 版本与环境治理（强制基线，2026-07-21 起）

本项目所有版本更新、打包、安装、环境切换和正式发布，必须遵守 [产品版本与环境治理基线](docs/production-version-governance.md)。

1. 版本、环境、分支为三条独立轴：版本使用 SemVer；环境固定为 `development` / `test` / `staging` / `production`；Git 初始化后使用受保护 `main`、短命 `feature/*` 和 `release/<version>`。
2. 必须 Build Once, Deploy Many：同一版本的 Test、Staging、Production 仅推广同一个 SHA-256 产物，禁止在不同环境重新编译。
3. 每个环境拥有独立的应用数据、连接资料、审计和账本目录；生产包默认使用 `production`，源码运行默认使用 `development`。
4. 环境模板仅含公开配置；令牌、密码、私钥与真实 SSH 资料只能在受保护的本机存储或外部密钥系统中注入。
5. 生产安装、网络暴露、真实 DGX 控制和版本晋级必须人工确认；构建成功不等于生产验收通过。
6. 每次候选包必须记录版本、hash、构建来源、验收结论和可回滚上一稳定包；完成全链路验收前不得声明为 `1.0.0`。

### 真实 DGX 验证原则（内测 0.1.0 起）

1. 当前内测环境以已验证 DGX 的真实状态和真实动作作为验收依据；不得以模拟结果、占位状态或假设代替实机结论。
2. 模型库只搜索当前已验证 SSH 连接上的本地已存在模型，不搜索公网模型库、不触发下载；搜索是固定只读发现动作，添加只登记可验证的模型元数据。
3. UI 中的“预热、启动、重启、停止、加载”只有在存在版本化固定适配器、容量预检、二次确认、执行进度、后置健康验证与审计时才可用；点击确认后的动作必须实际影响 DGX。
4. 已发现但尚无固定适配器的模型必须明确标记为“不可加载”，不得显示可用启动按钮或伪造成功。为新模型增加真实加载能力时，先交付其适配器、资源预算、回滚与实机验收，再开放 UI 控制。
5. 真实操作仍由 Codex 负责实施与验收；前端仅可按已批准的固定合同接入 UI，不获得任意命令、任意路径、凭据或浏览器终端能力。

### 通用新模型接入与受控加载（2026-07-22 起）

1. 模型库、服务配置、已管理服务与模型参数是四个独立层次：发现/添加不等于服务登记，服务登记不等于加载，参数只属于已管理服务。
2. 产品必须支持用户通过 UI 将 DGX 已配置且已发现的新模型纳入管理；流程固定为“发现 → 添加 → 选择受支持服务模板 → 预检 → 登记受控服务 → 计划/确认/真实执行/验证”。不得要求用户通过终端、聊天或手写命令完成正常使用流程。
3. 新模型服务配置只允许选择产品内置、版本化且已验证的服务模板。模板与固定适配器必须声明允许动作、资源预算、健康检查、回滚定义、兼容范围和内容摘要；不得接受任意命令、路径、端口、容器或环境变量。
4. “登记受控服务”、参数写入和服务启停属于真实 DGX 写入操作，必须各自经过授权、计划、二次确认、后置健康验证和本机审计。页面刷新、模型登记、连接恢复和客户端启动均不得自动启动模型。
5. 没有兼容模板或固定适配器的模型只能显示为“已发现/已添加，尚不能由产品加载”。产品 UI 不得保留或新增任何单一历史模型专属的通用入口、叙事或默认命名。
6. 详细工作分步、数据边界和验收门槛以 [通用模型接入与受控加载框架](docs/2026-07-22_通用模型接入与受控加载框架.md) 为准；实时状态同步进入任务看板。

## 操作进度反馈（强制规范，2026-07-21 起）

所有实际操作、验证、构建、安装、归档和外部状态检查结束后，必须按 [项目操作进度反馈规范](docs/progress-feedback-standard.md) 反馈“阶段与完成度、状态、本步、证据、剩余、下一步”。完成度只按可验收子项计算；代码、测试、安装和发布不得互相替代。任何受阻项必须明确写入任务架构。

> 实时进度、验证证据和阻塞项统一记录在 `02_实时任务状态看板.md`。本表中的状态必须随看板同步更新。

### 阶段 0：现状冻结

1. 记录现有端口、进程、容器和参数。
2. 备份 NVFP4、VLM、8091、8093 启动脚本。
3. 导出当前 API 冒烟测试结果。
4. 建立一键恢复脚本。
5. 确定控制台端口，建议 `8501`。

负责人：Codex。  
完成条件：任何后续修改失败时可以恢复当前服务。

### 阶段 1：UI 原型

页面结构：

- `/overview`：总览和模型状态卡片。
- `/models`：模型参数和预设。
- `/requests`：运行中及排队请求。
- `/logs`：日志和错误诊断。
- `/benchmark`：性能测试。
- `/settings`：API、权限和系统设置。

负责人：WorkBuddy。  
限制：只使用模拟数据，不连接 DGX，不实现重启按钮的真实操作。

### 阶段 2：只读监控

后端只读接口：

- `GET /api/health`
- `GET /api/services`
- `GET /api/system`
- `GET /api/models/nvfp4/metrics`
- `GET /api/models/vlm/metrics`
- `GET /api/requests`
- `GET /api/logs?service=nvfp4&lines=200`

负责人：Codex实现接口，WorkBuddy接入页面。  
完成条件：前端不使用模拟数据，且断线时显示明确错误。

### 阶段 3：参数编辑

参数分为两类：

#### 可即时生效

- `default_max_tokens`
- `fast_tool_mode`
- `enable_thinking_for_tools`
- `request_timeout`
- `concurrency_limit`

#### 必须重启

- `max_model_len`
- `kv_cache_memory`
- `max_num_seqs`
- `max_num_batched_tokens`
- `mtp_tokens`
- `prefix_caching`
- `gpu_memory_utilization`

UI 必须先显示变更差异、预计内存影响和是否需要重启。

### 阶段 4：安全控制

控制操作：

- 预热模型。
- 停止模型。
- 排空请求后重启。
- 立即重启。
- 常驻/按需模式切换。
- 应用配置。
- 回滚上一版本。

所有写操作必须：

1. 使用参数白名单。
2. 保存配置快照。
3. 写入操作审计日志。
4. 返回执行进度。
5. 失败时自动回滚。

负责人：Codex。

### 阶段 5：性能测试

测试模板：

- 单发短提示。
- 工具调用。
- 18K 长上下文冷/热缓存。
- 10/20/50 并发。
- NVFP4 与 VLM 同时运行。

输出：成功率、TTFT、总耗时、token/s、P50/P95/P99、内存峰值和错误分类。

### 阶段 6：部署验收

1. 控制台监听局域网地址和 `8501` 端口。
2. 配置登录口令或 API Token。
3. 使用 systemd 用户服务自启动。
4. 不修改现有 `8091/8092/8093` 对外契约。
5. 验证 WorkBuddy、Hermes、Open WebUI 继续可用。
6. 连续运行 24 小时，无内存泄漏和僵尸请求。

### 阶段 7：可安装产品与受控 UI 操作

1. 将现有前端和本机后台打包为 Windows 安装程序与桌面入口。
2. 首次运行以图形向导建立 SSH 连接配置、指纹确认和只读能力检查；凭据只由本机后台在 Windows 受保护存储中使用。
3. 为同类 DGX 部署提供版本化、固定动作的受控适配器；不提供浏览器终端或任意命令。
4. 将模型接入、预热、排空、切换、压测与回滚逐个迁移为“计划 → 二次确认 → 执行 → 验证 → 审计”的 UI 流程。
5. HY MT2 是首个试点：先接真实只读状态，再实现经过前置检查和回滚保护的切换事务。

负责人：Codex 负责本机后台、适配器、认证、安装与实机验收；WorkBuddy 负责向导、计划/确认/进度页面与前端测试。详细安全边界见 `docs/phase-7-productization-and-ui-control-plan.md`。

### 阶段 8：独立桌面应用体验与功能全覆盖

1. 用户从 Windows 桌面图标打开单一桌面应用，不需要手动启动 PowerShell、Vite 或浏览器。
2. Electron 主进程托管本机后台生命周期和受保护会话；renderer 不持有令牌、SSH 凭据、文件系统或子进程权限。
3. 所有已交付后台能力都必须有明确 UI 入口或可读状态：总览、连接、服务、模型、请求、日志与审计、性能、设置；首次连接保留为独立向导。
4. 所有控制动作继续遵循“计划 → 二次确认 → 进度 → 验证 → 审计”流程；不可执行能力必须可解释地禁用，而不是隐藏或伪造成功。
5. 交付包含托盘、登录启动、受控退出、升级恢复和桌面端到端验收。

负责人：Codex 负责桌面运行时、主进程/后台边界、受保护 IPC、安装升级与实机验收；WorkBuddy 负责标签页信息架构、页面壳、状态、可访问性与前端测试。详细拆分见 `docs/phase-8-native-desktop-app-plan.md`。

## 五、WorkBuddy 任务执行顺序

按以下顺序逐张执行：

1. `WB-01_项目脚手架.md`
2. `WB-02_静态总览界面.md`
3. `WB-03_模型参数页面.md`
4. `WB-04_日志与压测页面.md`
5. `WB-05_只读API接入.md`
6. `WB-10_HY-MT2待接入只读体验.md`（前五项完成后方可开始）
7. `WB-11_安装向导与受控模型切换体验.md`（C-09.1 完成后可开始 UI 原型）
8. `WB-12_安装向导真实只读连接接线.md`（C-09.2 合同交付后可开始）
9. `WB-13_桌面应用导航与功能页壳.md`（C-11.1 桌面运行时合同稳定后可开始）
10. `WB-14_外观语言常驻与快捷方式设置体验.md`（C-11.1、C-11.2 稳定后可开始）

WorkBuddy 每完成一张任务卡后必须停止，汇报修改文件和验证结果，等待验收后再进入下一张。

## 六、项目完成标准

- 页面样式接近桌面模型管理器，而不是普通后台表格。
- 真实状态刷新不影响推理性能。
- 参数变更前可预览、变更后可回滚。
- 危险操作有二次确认和进度反馈。
- 控制台故障不会影响 8093 API。
- 不在前端保存 SSH 密钥或 Docker 权限。
- 所有接口和操作都有日志。
- 同类环境安装后，用户可在软件内完成连接、兼容性检查和已授权的受控操作，不需要使用项目脚本或终端。
## DGX 远程桌面产品能力基线（C-15，2026-07-27）

1. 控制中心可增加 GNOME 系统级 RDP 的受控部署与 Windows RDP 启动能力，但只允许当前 active + verified OpenSSH profile，且不得提供任意远程命令或浏览器终端。
2. 远程桌面部署遵循“固定只读预检 → 生成独立 RDP 凭据 → 部署计划 → 二次确认 → 受限管理员助手/固定适配器 → 后置验证 → 审计/恢复”；Linux sudo 密码永不进入控制中心。
3. RDP 独立密码由 Electron 主进程使用系统加密随机源生成，并保存在 Windows 受保护存储；renderer 只在用户主动显示或复制时短时取得明文。密码不得进入 `.rdp`、命令参数、日志、审计或截图。
4. 默认以仅绑定 `127.0.0.1` 的 SSH 隧道连接 DGX 本机 3389，再启动 `mstsc.exe`；直接局域网暴露 3389 属于后续高级模式，必须单独说明范围、风险与回滚。
5. 产品不能绕过 GNOME 系统级部署所需的管理员授权。无受限管理员助手时，只能打开一次性 SSH 终端让用户手工输入 sudo 密码；完成摘要复核后再继续 UI 流程。
6. “服务 active / 3389 LISTEN / TCP 可达”不得冒充最终登录成功；只有用户在 Windows 凭据窗口输入 RDP 独立凭据并实际进入 GNOME 桌面后，才能标记远程操控验收通过。
7. 首次公开声明该功能前，必须完成真实 DGX 分阶段验收和持久 Windows 11 VM 空环境生命周期验收。

详细流程见 [C-15：DGX 远程桌面一键部署与 RDP 启动工作流](docs/2026-07-27_C15_DGX远程桌面一键部署与RDP启动工作流.md)。
